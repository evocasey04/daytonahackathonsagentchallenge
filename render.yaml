services:
  - type: web
    name: cyberagent-arena
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: DAYTONA_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
